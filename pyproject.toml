[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "chronelle"
version = "0.1.0"
description = "Persistent organizational state layer for AI-native organizations."
readme = "README.md"
requires-python = ">=3.9"
dependencies = []
